#ifndef CONVERSION_H
# define CONVERSION_H

#include <iostream>
#include <string>
#include <sstream>

class conversion {
	private:
		
	public:
		conversion();
		conversion(conversion const & src);
		conversion & operator=(conversion const & src);
		~conversion();


};



#endif